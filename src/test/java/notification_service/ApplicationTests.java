package notification_service;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest
class ApplicationTests {
	// This is the simplest possible Spring Boot test.
	// It tries to start the full application context (all beans, configs, etc.).
	// If the app cannot boot because of a wiring/configuration problem, this test
	// will fail.

	@Test
	void contextLoads() {
		// The test body is intentionally empty.
		// Success means: Spring Boot was able to start the application context.
	}

}
